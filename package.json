{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fe-dev": "cd packages/{{projectName}}-fe && npm run dev",
    "server-dev": "cd packages/{{projectName}}-server && npm run start:dev",
    "db-build": "cd packages/{{projectName}}-db && npm run db-build",
    "server-build": "cd packages/{{projectName}}-server && npm run build",
    "fe-build": "cd packages/{{projectName}}-fe && npm run build",
    "manage-build": "cd packages/{{projectName}}-manage && npm run build",
    "db-restart": "cd packages/{{projectName}}-db && npm run db:restart"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
